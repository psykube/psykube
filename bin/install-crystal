#!/usr/bin/env bash
set -e

case "$TRAVIS_OS_NAME" in
  linux)
    curl https://dist.crystal-lang.org/apt/setup.sh | sudo bash
    apt-get update
    sudo apt-get install crystal -y
    ;;
  osx)
    brew install crystal-lang
    ;;
  *)
    >&2 echo "ERROR: Unsupported OS"
    false
esac
